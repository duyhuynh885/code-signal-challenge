function adjacentElementsProduct(inputArray) {
    let max = 0;
    const arrLength
    for (let i = 0; i < arrLength - 1 ; i++) {
        if(i = arrLength - 1) return;
        if (i = 0 ) 
        if(max  < inputArray[arrLength]);
    }
}
